All commands for downloading, mapping, and variant calling 
the Dsim lines described in Jackson et al (2017) GBE, to
the Flybase version 2.02 version of the reference genome.

The mapping etc steps are in the base directory, and the
variant calling steps are in the GATK/ directory.

Scripts to download the raw read files from the nucleotide
archive are in the fastq/ directory.
